{
  "name": "artifact-registry-ui",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:ui": "concurrently -k -s first \"npm run dev\" \"wait-on http://localhost:3000 && node tests/smoke.test.js && node tests/login-flow.test.js\"",
    "test:lighthouse": "concurrently -k -s first \"npm run dev\" \"wait-on http://localhost:3000 && node tests/lighthouse.js\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "chromedriver": "^143.0.1",
    "concurrently": "^9.2.1",
    "lighthouse": "^12.8.2",
    "selenium-webdriver": "^4.39.0",
    "vite": "^5.0.8",
    "wait-on": "^9.0.3"
  }
}